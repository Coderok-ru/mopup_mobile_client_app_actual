# Навигация через Push-уведомления

Данный документ описывает, как настроить push-уведомления для автоматической навигации на определенные экраны приложения при нажатии на уведомление.

## Обзор

Приложение поддерживает автоматическую навигацию на различные экраны при нажатии на push-уведомление. Навигация определяется на основе данных, переданных в уведомлении.

## Формат данных в уведомлении

Данные для навигации передаются в поле `data` push-уведомления. Поддерживаются два способа указания целевого экрана:

1. **Через поле `type`** - указывает тип уведомления
2. **Через поле `screen`** - указывает конкретный экран напрямую

### Поддержка изображений

Приложение поддерживает отображение изображений в push-уведомлениях. Для этого добавьте в поле `data` уведомления:

- `image_url` или `image` - URL изображения для отображения в уведомлении

**Важно:** 
- Изображение должно быть доступно по HTTP/HTTPS
- Для Android изображение отображается в формате BigPicture
- Для iOS изображения поддерживаются через FCM attachments (см. примеры ниже)

## Поддерживаемые типы уведомлений

### 1. Уведомления о заказах

#### Переход на экран списка заказов

```json
{
  "type": "order"
}
```

или

```json
{
  "screen": "/orders"
}
```

#### Переход на детали конкретного заказа

```json
{
  "type": "order",
  "order_id": "123"
}
```

или

```json
{
  "type": "order_status",
  "order_id": "123"
}
```

или

```json
{
  "type": "order_created",
  "order_id": "123"
}
```

или

```json
{
  "type": "order_updated",
  "order_id": "123"
}
```

**Важно:** Если указан `order_id`, приложение автоматически откроет экран деталей заказа. Если `order_id` не указан, откроется список заказов.

### 2. Уведомления об оплате

```json
{
  "type": "payment"
}
```

или

```json
{
  "screen": "/payment"
}
```

### 3. Уведомления о настройках

```json
{
  "type": "settings"
}
```

или

```json
{
  "screen": "/settings"
}
```

### 4. Прямая навигация на экран

Можно указать экран напрямую через поле `screen`:

```json
{
  "screen": "/orders"
}
```

```json
{
  "screen": "/order-details",
  "order_id": "123"
}
```

```json
{
  "screen": "/payment"
}
```

```json
{
  "screen": "/settings"
}
```

```json
{
  "screen": "/main"
}
```

## Доступные экраны

| Маршрут | Описание |
|---------|----------|
| `/orders` | Список заказов |
| `/order-details` | Детали заказа (требуется `order_id`) |
| `/payment` | Экран оплаты |
| `/settings` | Настройки |
| `/main` | Главный экран |

## Примеры для разных платформ

### Firebase Cloud Messaging (FCM)

#### Пример для отправки через FCM API

```json
{
  "to": "FCM_TOKEN",
  "notification": {
    "title": "Статус заказа изменен",
    "body": "Ваш заказ #123 готов"
  },
  "data": {
    "type": "order_status",
    "order_id": "123"
  }
}
```

#### Пример с прямым указанием экрана

```json
{
  "to": "FCM_TOKEN",
  "notification": {
    "title": "Новый заказ",
    "body": "Ваш заказ успешно создан"
  },
  "data": {
    "screen": "/order-details",
    "order_id": "123"
  }
}
```

### Отправка через cURL

Да, вы можете отправить push-уведомление через cURL, используя данные из файла `google-services.json`. Для этого вам понадобятся:

1. **Project ID** из `google-services.json` (поле `project_id`)
2. **FCM Token** устройства (получается в приложении)
3. **Access Token** для авторизации (OAuth 2.0) или **Server Key** (Legacy API)

> **Никогда не храните ключ сервисного аккаунта в репозитории.**  
> Сгенерируйте ключ в Google Cloud Console и держите его только локально/в CI секрете.

#### Способ 1: Использование FCM API v1 (рекомендуется)

Для этого способа нужен OAuth 2.0 Access Token. Получить его можно через Google Cloud Console или используя gcloud CLI.

**Шаг 1: Получите Access Token**

```bash
# Установите gcloud CLI, если еще не установлен
# Затем выполните:
gcloud auth application-default print-access-token
```

Или получите токен через Google Cloud Console:
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект `mopup-client`
3. Перейдите в "APIs & Services" > "Credentials"
4. Создайте Service Account и получите JSON ключ
5. Используйте этот ключ для получения токена

**Шаг 2: Отправьте уведомление**

```bash
curl -X POST https://fcm.googleapis.com/v1/projects/mopup-client/messages:send \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": {
      "token": "FCM_TOKEN_УСТРОЙСТВА",
      "notification": {
        "title": "Статус заказа изменен",
        "body": "Ваш заказ #123 готов"
      },
      "data": {
        "type": "order_status",
        "order_id": "123"
      }
    }
  }'
```

**Важно:** Замените:
- `YOUR_ACCESS_TOKEN` на реальный OAuth 2.0 токен
- `FCM_TOKEN_УСТРОЙСТВА` на FCM токен, полученный в приложении

#### Способ 2: Использование Legacy FCM API (проще, но устаревший)

Для этого способа нужен Server Key из Firebase Console.

**Шаг 1: Получите Server Key**

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `mopup-client`
3. Перейдите в "Project Settings" (⚙️) > "Cloud Messaging"
4. Найдите "Server key" в разделе "Cloud Messaging API (Legacy)"
5. Скопируйте ключ

**Шаг 2: Отправьте уведомление**

```bash
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "FCM_TOKEN_УСТРОЙСТВА",
    "notification": {
      "title": "Статус заказа изменен",
      "body": "Ваш заказ #123 готов"
    },
    "data": {
      "type": "order_status",
      "order_id": "123"
    }
  }'
```

**Важно:** Замените:
- `YOUR_SERVER_KEY` на Server Key из Firebase Console
- `FCM_TOKEN_УСТРОЙСТВА` на FCM токен, полученный в приложении

#### Как получить FCM Token устройства

FCM Token выводится в консоль при запуске приложения. Также его можно получить программно:

1. Запустите приложение
2. В логах найдите строку: `🔑 FCM Token для отправки уведомлений:`
3. Скопируйте токен

Или добавьте в код приложения вывод токена в удобном месте для копирования.

#### Примеры полных команд cURL

**Пример 1: Уведомление о заказе с переходом на детали**

```bash
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "FCM_TOKEN_УСТРОЙСТВА",
    "notification": {
      "title": "Заказ создан",
      "body": "Ваш заказ #123 успешно создан"
    },
    "data": {
      "type": "order_created",
      "order_id": "123"
    }
  }'
```

**Пример 2: Уведомление с переходом на экран оплаты**

```bash
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "FCM_TOKEN_УСТРОЙСТВА",
    "notification": {
      "title": "Требуется оплата",
      "body": "Оплатите заказ #123"
    },
    "data": {
      "type": "payment"
    }
  }'
```

**Пример 3: Уведомление с прямым указанием экрана**

```bash
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "FCM_TOKEN_УСТРОЙСТВА",
    "notification": {
      "title": "Новое уведомление",
      "body": "Перейти к заказам"
    },
    "data": {
      "screen": "/orders"
    }
  }'
```

#### Данные из google-services.json

Из вашего файла `google-services.json` можно использовать:

- **Project ID:** `mopup-client` (для FCM API v1)
- **Project Number:** `675430111158`
- **Package Name:** `com.coderok.mopup`

**Примечание:** API ключ в `google-services.json` (`AIzaSyClArd6j3dGRNKAPRyl4lq3jC-e88tHi2c`) - это клиентский ключ для использования в приложении, он не подходит для отправки уведомлений с сервера. Для серверной отправки нужен Server Key или OAuth 2.0 токен.

## Логика обработки навигации

Приложение обрабатывает навигацию в следующем порядке:

1. **Если указано поле `screen`** - выполняется переход на указанный экран
2. **Если указано поле `type`**:
   - `order`, `order_status`, `order_created`, `order_updated` → если есть `order_id`, открывается детали заказа, иначе список заказов
   - `payment` → экран оплаты
   - `settings` → экран настроек
   - другие типы → главный экран
3. **Если указан только `order_id`** (без `type` и `screen`) → открывается детали заказа
4. **Если ничего не указано** → открывается главный экран

## Особенности работы

### Открытие приложения из уведомления

Если приложение было закрыто и открыто через уведомление, навигация выполняется автоматически после инициализации приложения.

### Нажатие на уведомление в фоне

Если приложение находится в фоне и пользователь нажимает на уведомление, выполняется переход на соответствующий экран.

### Нажатие на уведомление на переднем плане

Если приложение открыто и приходит уведомление, при нажатии на него также выполняется навигация.

## Рекомендации

1. **Всегда указывайте `order_id`** для уведомлений о заказах, чтобы пользователь мог сразу перейти к деталям
2. **Используйте поле `type`** для стандартных типов уведомлений
3. **Используйте поле `screen`** для нестандартных переходов или когда нужен прямой контроль
4. **Тестируйте навигацию** на разных сценариях (приложение закрыто, в фоне, открыто)

## Отладка

Для отладки навигации проверьте логи приложения. При обработке уведомления выводятся следующие сообщения:

- `🔔 Обработка навигации из уведомления: {данные}`
- `📱 Навигация на детали заказа: {order_id}`
- `📱 Навигация на экран заказов`
- `📱 Навигация на экран оплаты`
- `📱 Навигация на экран настроек`
- `📱 Навигация на главный экран`

## Примеры полных уведомлений

### Уведомление о создании заказа

```json
{
  "notification": {
    "title": "Заказ создан",
    "body": "Ваш заказ #123 успешно создан"
  },
  "data": {
    "type": "order_created",
    "order_id": "123"
  }
}
```

### Уведомление об изменении статуса заказа

```json
{
  "notification": {
    "title": "Статус заказа изменен",
    "body": "Ваш заказ #123 выполнен"
  },
  "data": {
    "type": "order_status",
    "order_id": "123"
  }
}
```

### Уведомление об оплате

```json
{
  "notification": {
    "title": "Требуется оплата",
    "body": "Оплатите заказ #123"
  },
  "data": {
    "type": "payment"
  }
}
```

### Уведомление с прямым указанием экрана

```json
{
  "notification": {
    "title": "Новое уведомление",
    "body": "Перейти к заказам"
  },
  "data": {
    "screen": "/orders"
  }
}
```

### Уведомление с изображением

#### Для Android (через data поле)

```json
{
  "notification": {
    "title": "Новый заказ",
    "body": "Ваш заказ #123 готов"
  },
  "data": {
    "type": "order_status",
    "order_id": "123",
    "image_url": "https://example.com/image.jpg"
  }
}
```

или

```json
{
  "notification": {
    "title": "Новый заказ",
    "body": "Ваш заказ #123 готов"
  },
  "data": {
    "type": "order_status",
    "order_id": "123",
    "image": "https://example.com/image.jpg"
  }
}
```

#### Для iOS (через FCM attachments)

Для iOS изображения должны быть указаны в поле `fcm_options`:

```json
{
  "message": {
    "token": "FCM_TOKEN",
    "notification": {
      "title": "Новый заказ",
      "body": "Ваш заказ #123 готов"
    },
    "data": {
      "type": "order_status",
      "order_id": "123",
      "image_url": "https://example.com/image.jpg"
    },
    "fcm_options": {
      "image": "https://example.com/image.jpg"
    }
  }
}
```

**Примечание:** Для iOS изображения автоматически добавляются в уведомление, если они указаны в `fcm_options.image`. Для Android изображение загружается из поля `image_url` или `image` в `data`.

